{
    "name": "pd-webapp",
    "version": "1.0.0",
    "scripts": {
        "start": "serve",
        "build": "browserify -i nw.gui --s pdbundle browserify_entries.js -o dist/bundle.js"
    },
    "devDependencies": {
        "browserify": "^16.5.2",
        "serve": "^11.3.2"
    }
}
